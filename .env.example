# Database (Neon PostgreSQL) — used in the next phase (auth + Prisma)
DATABASE_URL="postgresql://postgres:0208@localhost:5432/tata1mg?schema=public"
# Auth.js (NextAuth)
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="replace-with-a-random-secret"
JWT_SECRET="replace-with-a-random-secret"
